<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Gloria+Hallelujah&family=Titan+One&display=swap"
      rel="stylesheet">
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css
     "> 
    <link rel="stylesheet" type="text/css" href="styles/main.css">
</head>
<body>
    

<div class="container categorizedNews">
    <% if(locals.content){ %>
        <h4 class="news-header"> Total  <%= content.length  %> news  </h4>
        <div class="news-section">
            <div class="row row-cols-auto">
                <% for (let i = 0 ; i <content.length ; i++ ){  %>
                <div class="card text-center mb-3" style="width: 18rem;">
                    <div class="card-body">
                          
                      <h5 class="card-title"><%= content[i]["name"]  %></h5>
                      <p class="card-text"><%= content[i]["description"]  %>  </p>
                     <button type="button" class="btn btn-info ">
                      <a href="<%= content[i]["url"]  %>" class="">Read more</a>
                    </button>
                    </div>
                  </div>
                  
                  <% } %>


            </div>
        </div>
    </div>

        <% } else{ %>
    <h3> This is news categories page  </h3>
<% } %>

  <script async src="https://cdn.jsdelivr.net/npm/es-module-shims@1/dist/es-module-shims.min.js" crossorigin="anonymous"></script>
